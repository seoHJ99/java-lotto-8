# 로또 발매기

## 목적
로또 자동 발행 프로그램.
사용자가 입력한 구입액만큼 로또를 발행하고, 당첨번호를 입력받아 최종 결과와 수익률을 출력한다.

## 조건
- 로또 범위는 1~45사이다.
- 한번에 6개 숫자와 1개의 보너스 숫자를 뽑는다. 숫자는 중복되지 않는다.
- 로또 구입 금액을 입력하면 그에 해당하는 개수만큼 로또를 발행한다.
- 로또 하나에 1000원이다.
- 1000원 단위로 나눠떨어지지 않는 경우는 예외 처리한다.
- 사용자로부터 당첨번호 6개와 보너스 번호 1개를 입력받아, 당첨 내역을 구한다.
- 로또 당첨되기 위해서 순서까지 일치할 필요는 없다.
- 총 수익율은 소수점 둘째자리에서 반올림하여 보여준다.
- 로또 번호는 오름차순으로 보여준다.
- 사용자로부터 잘못된 값을 입력받으면 IllegalArgumentException 를 발생시키고 [ERROR] 로 시작하는 메세지를 출력한다.
- [ERROR] 메세지 출력 이후부터 다시 입력받는다. 프로그램은 멈추지 않는다.
- 당첨 금액과 기준은 아래 표와 같다.

| 등수 | 당첨 조건 | 당첨금 |
|:----:|:--------------------------|----------------:|
| 1등 | 6개 번호 일치 | 2,000,000,000원 |
| 2등 | 5개 번호 + 보너스 번호 일치 | 30,000,000원 |
| 3등 | 5개 번호 일치 | 1,500,000원 |
| 4등 | 4개 번호 일치 | 50,000원 |
| 5등 | 3개 번호 일치 | 5,000원 |

## 프로그래밍 요구사항
- 메서드 길이는 15줄 이내로 한정한다.
- else를 사용하지 않는다.
- Enum 을 사용한다.
- 출력문을 제외하고 단위테스트를 작성한다.
- Lotto 클래스를 이용해서 구현한다. 필드를 추가할수 없다.

## 접근법
- 로또를 생성하는 역할을 하는 클래스가 필요하다.
- 로또를 저장하는 클래스가 필요하다.
- 당첨번호를 저장할 공간이 필요하다. 별도의 클래스로 만들지는 고민.
- 당첨을 확인하는 클래스가 필요하다.
- 통계를 내는 클래스가 필요하다.

## 주요 기능
- 로또 번호 생성
- 로또 추가 번호 생성
- 사용자 입력값 받아서 로또 발행
- 당첨 번호 비교 및 당첨금, 수익률 계산

## 예외 상황
- 사용자가 1000원 단위로 나눠떨어지지 않는 금액을 입력한다.
- 사용자가 숫자가 아닌 값을 입력한다.
- 사용자가 당첨 번호를 1~45 이외의 문자를 입력한다.
- 사용자가 빈 값을 입력한다.
- 사용자가 2^31 (2,147,483,647) 이상의 숫자를 입력한다.
